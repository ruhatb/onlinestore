package onlinestore.onlinestore.repository;

import onlinestore.onlinestore.entity.SelectOrder;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SelectOrderRepository extends JpaRepository<SelectOrder, Long> {
}
