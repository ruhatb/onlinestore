package onlinestore.onlinestore.repository;

import onlinestore.onlinestore.entity.SelectCart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SelectCartRepository extends JpaRepository<SelectCart, Long> {
}
